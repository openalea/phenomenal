language: cpp

os:
  - linux
  - osx

osx_image: xcode11.2

sudo: required

services:
  - docker

env:
  - CONDA_RECIPE=conda
    CONDA_VERSION=3

install:
  - git clone https://github.com/openalea/travis-ci.git
  - cd travis-ci
  - source install.sh
  - conda config --add channels conda-forge
  - conda info
  - conda install -y numpy --force-reinstall
  - conda install -y numpy-base --force-reinstall
  - conda install -y scipy --force-reinstall


before_script:
  - source before_script.sh

script:
  - source script.sh

after_success:
  - source after_success.sh

after_failure:
  - source after_failure.sh

before_deploy:
  - source before_deploy.sh

deploy:
  skip_cleanup: true
  provider: script
  script: bash deploy_script.sh

after_deploy:
  - source after_deploy.sh

after_script:
  - source after_script.sh
